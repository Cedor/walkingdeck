{
  "name": "the-walking-deck-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Automated client-side tests for The Walking Deck.",
  "scripts": {
    "lint": "node --check thewalkingdeck.js && node --check scripts/extract-client-translations.mjs && node scripts/validate-json.mjs",
    "test": "node --test tests/js/*.test.js",
    "translations:extract": "node scripts/extract-client-translations.mjs"
  }
}
